
import 'package:flutter/material.dart';

AppBar buildAppBar() {
  return AppBar(
    actions: [
      IconButton(
        onPressed: () {},
        icon: Icon(Icons.account_circle),
        iconSize: 30,
      ),
      IconButton(
        onPressed: () {},
        icon: Icon(Icons.menu),
        iconSize: 30,
      ),
    ],
  );
}
